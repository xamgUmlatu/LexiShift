# Project Integrity Secondary Pass Notes

Status: active notes
Role: Planning / WIP
Last updated: 2026-04-19
Last verified: 2026-04-19 E1.2 generic translation heuristic narrowing plus semantic locator naming carry-forward capture
Purpose: capture interesting observations discovered during the secondary pass that are not fixed or fully resolved in the current slice
Source-of-truth: notes ledger only; entries are reminders and hypotheses, not verified product truth unless promoted into code, tests, `feature_state_matrix.md`, or another canonical doc
Related docs:
- `project_integrity_secondary_pass_plan.md`
- `project_integrity_stabilization_backlog.md`
- `project_integrity_stabilization_runbook.md`
- `feature_state_matrix.md`

## How To Use This Ledger

Add an entry when:

- the observation is relevant to integrity/correctness,
- it is not the immediate slice being executed,
- and it would be easy to forget if it only lived in chat history.

Each entry should be specific enough that a later pass can pick it up without re-discovering it from scratch.

Recommended status values:

- `carry-forward`: worth reviewing in a later slice
- `promoted`: moved into a canonical doc/backlog item/test task
- `resolved`: addressed directly in code/docs/tests
- `dismissed`: reviewed later and determined not to require action

Recommended category values:

- `contract mismatch`
- `persistence drift`
- `lifecycle bug`
- `negative-path gap`
- `docs/state drift`
- `false-green evidence`
- `tooling noise`

## Entries

| ID | Date | Found during | Category | Observation | Why it matters | Suspected scope | Status | Next action | Promotion target |
|---|---|---|---|---|---|---|---|---|---|
| `N-001` | 2026-04-17 | SRS quality harness refresh | `contract mismatch` | Due-aware publication still reports a warning: published ruleset covers more items than the due subset in at least one synthetic scenario. | This mattered because the system had a due queue and due-aware design docs, but helper publication/runtime gating still operated on the broader admitted inventory. | `SP2` SRS publication/runtime semantics | `promoted` | `SP2.4` promoted this into explicit current truth: due-aware serving remains planned, while the harness keeps broader-than-due publication as a warning instead of a pass claim. | `feature_state_matrix.md`, `project_integrity_sp2_due_aware_serving_packet.md`, SRS practice docs |
| `N-002` | 2026-04-17 | `npm --prefix scripts run check:changed` | `tooling noise` | The repo-safety loop still reports `changed_style_status: advisory-fail` locally because `.venv/bin/python` cannot import `ruff`. | This reduces signal quality and makes it easier to ignore meaningful validation output. | `SP7` tooling reliability | `carry-forward` | Decide whether the right fix is local environment setup, script hardening, or explicit workflow guidance. | local setup docs or repo-safety tooling |
| `N-003` | 2026-04-17 | Secondary-pass planning | `docs/state drift` | The stabilization backlog's structural-health snapshot still reflects the earlier near-limit state and clean-worktree checkpoint, not the current branch after the recent splits and generated evidence refreshes. | Planning docs can become misleading if the snapshot looks current after the branch reality has changed. | `SP6` backlog reconciliation | `carry-forward` | Refresh the snapshot once the next documentation/state reconciliation slice is ready. | `project_integrity_stabilization_backlog.md` |
| `N-004` | 2026-04-18 | `SP1.5` secondary resource compatibility review | `contract mismatch` | `wordnet-en` and `moby-en` now participate in the shared language-resource binding/manual-path map in the settings panel, but downstream consumers still rely on the dedicated `wordnet_dir` and `moby_path` fields rather than a fully uniform secondary-pack contract. | Future normalization or UX refactors can update one representation and accidentally leave the other behind unless this exception stays explicit. | later secondary lexical normalization and runtime-consumer reconciliation | `carry-forward` | When the secondary lexical family decision is made, decide whether these two sources become normal managed/manual pack entries everywhere or remain documented permanent exceptions. | `data_source_normalization_execution_order.md`, `feature_state_matrix.md`, or a later SP1/SP5 packet |
| `N-005` | 2026-04-18 | `SP2.2` extension SRS settings contract audit | `negative-path gap` | `saveSrsSettings()` still uses separate profile persistence, runtime publish, and signal persistence writes, but `SP2.6` now routes settings changes through the shared async error handler and marks late failures as partial-save errors instead of leaving that state implicit. | This closed the immediate operator-visibility gap without forcing a mid-pass transactional redesign. | `SP2.6` settings save failure visibility | `resolved` | Revisit only if later UX work broadens the editable save surface enough that explicit messaging stops being sufficient. | `project_integrity_sp2_settings_save_failure_packet.md`, `project_integrity_secondary_pass_plan.md` |
| `N-006` | 2026-04-18 | `D3` active inventory persistence audit | `negative-path gap` | Explicit active inventory resolution is intentionally forgiving: missing inventory falls back to store-derived membership and stale inventory ids are silently dropped during resolution. `SP2.7` now promotes that model into canonical docs instead of leaving it mostly implicit in code, diagnostics, and older checkpoint packets. | This closed the immediate doc-drift risk while preserving the option to add a stronger repair/reporting model later if product needs change. | `SP2.7` active-inventory observability truth | `resolved` | Revisit only if later lifecycle work needs a dedicated drift-repair artifact or a harder inventory authority model. | `project_integrity_sp2_inventory_observability_packet.md`, `feature_state_matrix.md`, `srs_practice_layer_design.md` |
| `N-007` | 2026-04-18 | D7-D8 synthesis checkpoint | `false-green evidence` | Two recent seams showed the same pattern: present-tense validation claims were stronger than the committed direct evidence behind them. D7 needed explicit LP E2E assertions for manifest-family coherence, and D8 found a runbook note already citing `core/tests/dev/test_extension_srs_action_workflows.py` before that test existed in-tree. | This matters because seam docs and checkpoint notes can look fully verified while still relying on implicit coverage or local-only test state. | `G2` evidence hygiene and future seam checkpoint notes | `carry-forward` | When a seam doc cites a validation bundle, prefer committed seam-local tests or assertions over implied coverage, and audit older checkpoint docs for similar doc-ahead-of-evidence cases during `G2`. | `project_integrity_secondary_pass_plan.md`, later `G2` packet, or checkpoint docs that still cite implicit coverage |
| `N-008` | 2026-04-18 | `E1.1` translation-pack tooling holdout audit | `contract mismatch` | `rulegen_probe_words.py` now keeps raw paths as execution details/manual override inputs, while also surfacing translation-pack and source-frequency-pack identity/provider/POS-profile metadata in stdout and JSON so the probe contract matches the benchmark/runtime seams more closely. | This mattered because later UX or docs work could otherwise mistake probe-local path fields for the real managed-pack contract even though runtime and benchmark artifacts were already manifest/pack-id oriented. | `N-008` probe-surface cleanup | `resolved` | Follow-up complete: the remaining holdout is no longer the probe surface itself but the lower pair-config `freedict_*` field names now tracked separately. | probe resource-payload tests plus this notes ledger |
| `N-009` | 2026-04-18 | `E2.1` frequency-pack tooling and diagnostics audit | `contract mismatch` | Helper CLI/native-host execution APIs still use the path-first `set_source_db` field name even though settings persistence and runtime diagnostics now expose frequency-pack identity separately. | This mattered because later docs or UI cleanup could over-read `set_source_db` as the canonical managed frequency contract when it is really an execution-layer file override. | later `E4` installed-vs-manual cleanup plus possible helper API cleanup in `F12` | `resolved` | Helper CLI now exposes `--frequency-pack-path` while keeping `--set-source-db` as a compatibility alias, native-host requests accept `frequency_pack_path`, and preview/rebalance payloads now surface frequency-pack path/id/provider/POS-profile fields alongside the legacy execution field. | helper/native-host frequency alias tests plus preview/rebalance payload contract tests |
| `N-010` | 2026-04-18 | `E4.1` installed/manual contract cleanup | `contract mismatch` | The extension-side SRS action formatters still print raw `set_source_db` lines in preflight and diagnostics output without the same explicit “installed packs by default / manual override” framing now used in helper CLI copy and settings surfaces. | This mattered because operator-facing diagnostics could still read as path-first even though the underlying settings/runtime contract is pack-first for managed installs. | later extension diagnostics wording follow-through, likely alongside `F14` controller cleanup | `resolved` | The extension SRS action formatters now surface `frequency_pack_id` / provider / POS profile / pack path and label `set_source_db` as an execution-layer field instead of presenting it alone as the primary contract. | extension diagnostics copy and targeted formatter tests |
| `N-011` | 2026-04-18 | `F11.1` dict loader sqlite-support split | `tooling noise` | `dict_translation_grouped_loader.py` now reuses the extracted sqlite schema probes, but it still carries its own near-duplicate auxiliary-sqlite grouped query and metadata-hydration path instead of sharing the new record-loading seam. | This matters because future auxiliary-sqlite row-shape or metadata changes could still need parallel updates in two low-level loaders, raising drift risk even after the helper extraction landed. | later resource-loader consolidation after the current hotspot extractions | `carry-forward` | Decide later whether the grouped-by-translation loader should share more of the sqlite-support seam or remain a documented parallel path for translation-grouped reads. | later `F11` follow-up packet, `project_health_remediation_workstream.md`, or a resource-loader seam map |
| `N-012` | 2026-04-19 | resource-download follow-up planning | `negative-path gap` | Pack-download URLs and archive fallbacks are still hardcoded in `language_packs_catalog.py`, while downloader threads and the transfer mixin collapse most failures into raw exception strings plus one generic archive hint. | This matters because upstream source churn currently requires a code/update cycle and the current UX cannot cleanly distinguish offline/source-side/local-build failures. | later `SP1` download lifecycle follow-through | `promoted` | Keep the bundled catalog as the code-owned schema, add a remote source-manifest overlay for mutable transport fields, and add explicit failure classification before the settings-panel copy layer. | `project_integrity_sp1_data_source_download_packet.md` |
| `N-013` | 2026-04-19 | SP1 closure review | `tooling noise` | Additional URL-manifest sophistication such as disabled-aware audit semantics, checksum enforcement, scheduled CI checks, or broader mirror management is not justified right now because pack-source URL changes are expected to be rare and the current audit is only a manual development check. | This matters because otherwise a narrow operational escape hatch can quietly turn into a maintenance-heavy subsystem without product need. | later SP1 follow-up only if real source churn appears | `dismissed` | Reopen only if URL churn, automated monitoring, or repeated operator confusion makes the simple manual-audit workflow insufficient. | `project_integrity_sp1_data_source_download_packet.md` |
| `N-014` | 2026-04-19 | `N-008` probe contract cleanup | `contract mismatch` | The probe surface is now pack-aware, but the pair-local rulegen config fields below it still use provider-shaped names such as `freedict_es_en_path` and `freedict_de_en_path`. | This matters because the remaining confusion risk has moved below the developer-tool seam: later internal cleanup work could still misread those pair-config names as part of the generic managed-pack contract. | later internal translation seam convergence (`F7` / `F11`) | `carry-forward` | Keep pair-local provider names explicit as an internal implementation detail until a later structural slice decides whether they should be renamed or permanently documented as provider-local seams. | later translation internal-convergence packet or `data_source_normalization_execution_order.md` |
| `N-015` | 2026-04-19 | `E1.2` generic translation heuristic review | `contract mismatch` | `lp_capabilities.py` still exposes semantic publication locator modes and reason codes such as `freedict_gloss` / `missing_freedict_gloss_locator` even though the surrounding generic translation-pack contract has been moving away from provider-shaped naming. | This matters because diagnostics or docs work above the helper seam can still absorb provider-shaped vocabulary from these capability constants and treat it as part of the generic managed-pack contract. | later semantic-publication naming cleanup after translation helper heuristics settle | `carry-forward` | Revisit when the semantic publication/runtime contract is next touched and decide whether those locator names should be generalized or documented as intentionally provider-local semantics. | later semantic contract packet or `feature_state_matrix.md` if current truth claims start depending on those names |
